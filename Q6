def can_shift_to_goal(s, goal):
    if len(s) != len(goal):
        return False

    return goal in s + s

# Test Example 1
s = "abcde"
goal = "cdeab"
print(can_shift_to_goal(s, goal))  # Output: True
